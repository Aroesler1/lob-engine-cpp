[project]
name = "lob-engine-cpp"
version = "0.1.0"
description = "Deterministic C++ limit order book engine for LOBSTER-style message data"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
