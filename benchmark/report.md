# Benchmark Report Status

No benchmark executable, script, dataset harness, or measured throughput results are present in this checkout.

Current benchmark-related directories:

- `benchmark/` contains this status file plus a placeholder `.gitkeep`
- `report/` contains a placeholder `.gitkeep`

As a result, there are no checked-in numbers to report for:

- ticker-level throughput
- single-core parsing rates
- compiler/build-mode benchmark comparisons
- `std::map` versus flat-vector container tradeoffs

This file exists so the top-level README can point to a stable benchmark-report path without implying that benchmark data has already been produced.
